Generated by scripts/run_part4.py under MOCK_LLM with zero API keys and
zero outbound network access. Reproduce with: .venv/bin/python scripts/run_part4.py

PART 4 TASKS 15 AND 16 - RESILIENCE

DEMONSTRATION 1 - THE RETRY POLICY RECOVERS A TRANSIENT FAILURE

shim: agent.tools.check_loan_application_status raises ConnectionError
on its first two calls, then answers for real on the third. It lives
here, not in the graph (D-76).

retry policy (agent/graph.py::RETRY_POLICY, attached to 'lookup_status'):
  max attempts    = 3
  initial interval= 0.05s
  max interval    = 0.4s
  backoff factor  = 2.0
  jitter          = False

  attempt 1: raised ConnectionError('simulated transient failure 1')
  attempt 2: raised ConnectionError('simulated transient failure 2')
  attempt 3: succeeded, found=True

attempts spent: 3 of 3 allowed
route: lookup   found: True

DEMONSTRATION 2 - A NODE OVER ITS OWN BUDGET RAISES CLEANLY

shim: agent.tools.check_loan_application_status sleeps past a 0.1s per-node budget (config.NODE_TIMEOUT_SECONDS is 5.0s normally, narrowed here so the demo does not wait on a real timeout) instead of doing the record read it pretends to do.

exception type: NodeTimeoutError
configured per-node budget for this demonstration: 0.1s (config.NODE_TIMEOUT_SECONDS is 5.0s normally)
message: Node 'lookup_status' exceeded its run timeout of 0.100s.

DEMONSTRATION 3 - A RUN OVER THE GLOBAL BUDGET IS CANCELLED

shim: agent.tools.check_loan_application_status sleeps 1s, well inside its own per-node budget (5.0s) so only the whole-run budget can be the one that fires. config.GRAPH_TIMEOUT_SECONDS is narrowed to 0.2s for this demonstration only (normally 30.0s).

asyncio.wait_for around ainvoke (agent/graph.py::ask_async) raised
TimeoutError before the node's sleep(1) returned: the whole run was
cancelled by the global budget, not by the per-node one.
